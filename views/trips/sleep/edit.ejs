<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel='stylesheet' href="/stylesheets/reset.css" />
    <link rel='stylesheet' href="/stylesheets/newtripdetails.css" />
    <title>Travel App</title>
</head>
<body>
<nav>
    <p><%= trip.tripName %> - Edit <%= sleep.placeName %></p>
    <a href="/trips/<%= trip._id %>">Back</a>
</nav>
    <main>
        <form action="/tripdetails/sleep/<%= sleep._id %>?_method=PUT" method="POST">
            <div>
                <label>Name: </label>
                <input type="text" name="placeName" value="<%= sleep.placeName %>">
            </div>
            <div>
                <label>City: </label>
                <input type="text" name="city" value="<%= sleep.city %>">
            </div>
            <div>
                <label>Country: </label>
                <input type="text" name="country" value="<%= sleep.country %>">
            </div>
            <div>
                <label>Average Cost: </label>
                <input type="number" name="cost" value="<%= sleep.cost %>">
            </div>
            <div>
                <label>Rating: </label>
                <input type="number" name="rating" value="<%= sleep.rating %>">
            </div>
            <div>
                <label>From (<span><%= sleep.startDate.toDateString() %></span>)</label>
                <input type="date" name="startDate">
            </div>
            <div>
                <label>To (<span><%= sleep.endDate.toDateString()%></span>) </label>
                <input type="date" name="endDate">
            </div>
            <div>
                <input type="submit" value="Update">
            </div>
        </form>
    </main>
</body>
</html>