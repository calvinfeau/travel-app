<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel='stylesheet' href="/stylesheets/reset.css" />
    <link rel='stylesheet' href="/stylesheets/newtripdetails.css" />
    <title>Travel App</title>
</head>
<body>
    <nav>
        <p><%= trip.tripName %> - Edit <%= food.foodSpot %></p>
        <a href="/trips/<%= trip._id %>">Back</a>
    </nav>
    <main>
        <form action="/tripdetails/food/<%= food._id %>?_method=PUT" method="POST">
            <div>
                <label>Name: </label>
                <input type="text" name="foodSpot" value="<%= food.foodSpot %>">
            </div>
            <div>
                <label>City: </label>
                <input type="text" name="city" value="<%= food.city %>">
            </div>
            <div>
                <label>Country: </label>
                <input type="text" name="country" value="<%= food.country %>">
            </div>
            <div>
                <label>Average Cost: </label>
                <input type="number" name="cost" value="<%= food.cost %>">
            </div>
            <div>
                <label>Rating: </label>
                <input type="number" name="rating" value="<%= food.rating %>">
            </div>
            <div>
                <label>Date (<span><%=food.day.toDateString()%></span>): </label>
                <input type="date" name="day">
            </div>
            <div>
                <input type="submit" value="Update">
            </div>
        </form>
    </main>
</body>
</html>